<script>
$('#datepicker').datepicker({format: 'yyyy-mm-dd'});
</script>


<%= simple_form_for @burfday ||= Burfday.new, remote: true do |f| %>
<% if @burfday.errors.any? %>
<div id="error_explanation">
  <%= pluralize(@burfday.errors.count, "error") %> prohibited this #BURFDAY from being saved:

  <ul>
    <% @burfday.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <%= f.hidden_field :user_id, value: current_user.id %>

<div class="form-group">
  <%= f.label :name, class: "col-sm-2 control-label" %>
  <%= f.input_field :name, class: "form-control", placeholder: "Full Name" %>
</div>
<div class="form-group">
  <%= f.label :age, class: "col-sm-2 control-label" %>
  <%= f.text_field :age, class: "form-control", id: "datepicker", placeholder: "#BURFDAY" %>
</div>

<div class="form-group">
  <%= f.label :photo, class: "col-sm-2 control-label" %>
  <%= f.input_field :photo, class: "form-control" %>
</div>

<!-- <div class="form-group">
  <% f.input_field :email, class: "form-control", placeholder: "Their Email Address" %>
</div> -->

  <%= f.button :submit, :class => 'btn-success' %>

<% end %>

<script>
$('#datepicker').datepicker({format: 'yyyy-mm-dd', yearRange: "-10:0"});
</script>
